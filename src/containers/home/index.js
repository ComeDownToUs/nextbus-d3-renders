import React from "react";

const Home = props => (
  <div>
    This is a placeholder home page that would likely just provide an alternate
    means of navigation than the map
  </div>
);

export default Home;
